<% provide(:search_bar, @search_name[:search_name]) %>
<h1>Your queries</h1>
<% if @products.empty? %>
  You haven't done any questions yet
<% else %>
  <div class="overflow-hidden mb-4">
    <div class="row gy-4">
      <%= render @products, list: false %>
      <%== pagy_bootstrap_nav(@pagy) %>
    </div>
  </div>
<% end %>
