<% if @products.empty? %>
  No Favorites added yet
<% else %>
  <div class="container overflow-hidden mb-4">
    <div class="row gx-3 gy-4">
      <%= render @products %>
    </div>
  </div>
<% end %>